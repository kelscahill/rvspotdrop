{% if footer_legal_nav.items %}
  <nav class="c-nav-footer-legal" role="navigation">
    {% for item in footer_legal_nav.items %}
      <a class="c-nav-footer-legal__link" target="{{ item.target ? item.target : '_self' }}" href="{{ item.link }}">{{ item.title }}</a>
    {% endfor %}
  </nav>
{% endif %}